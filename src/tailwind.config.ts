// This file is obsolete. The main configuration is in the root `tailwind.config.ts`.
// Keeping duplicate configuration files can cause unexpected build errors.
export {}; // Add an empty export to ensure it's treated as a module.
